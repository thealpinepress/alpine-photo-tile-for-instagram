$ = jQuery = require('jquery');

const menu_script =  require("../js/AlpinePhotoTiles_menu_script")

describe("AlpinePhotoTiles menu script tests", () => {

    test("tests compilation of javascript against latest jQuery", () => {
        // given

        // when
        // calling function b

        expect(true).toEqual(true);
    });
});